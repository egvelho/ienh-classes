info:
  name: Push
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/push
  body:
    type: json
    data: |-
      {
        "value": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
