# Entregável 2.3

- Implemente os métodos de geolocation.controller de acordo com as solicitações.
- Siga as orientações descritas nos arquivos.
- No arquivo geolocation.model, você deve guardar uma lista de coordenadas no formato:

```javascript
{ latitude: number, longitude: number }[]
```

É OBRIGATÓRIO UTILIZAR LISTA ENCADEADA!

- Implemente os status HTTP corretamente (utilize 404, 200 e 201)
- Implemente os métodos HTTP corretamente (nos métodos que não estão implementados)

**Lembre-se: o controller invoca o service que invoca a model!**

Mesmos grupos que vocês já estão! Entrega pelo Classroom (somente link do GitHub). Somente um membro do grupo entrega (não esqueça de colocar os nomes!)
