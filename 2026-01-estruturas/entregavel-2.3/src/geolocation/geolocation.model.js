export class GeolocationModel {
  constructor() {
    this.quantTestes = 0; // Me apague
    //this.linkedList = new LinkedList();
  }

  // Me apague
  teste() {
    console.log(
      `Método teste do geolocation.model! Fui invocado ${++this.quantTestes} ${this.quantTestes === 1 ? "vez" : "vezes"}`,
    );
  }
}
